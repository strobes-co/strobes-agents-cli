"""Strobes Agents AI — local terminal client for remote Strobes workspaces.

A Claude-Code-style CLI that binds to a remote Strobes organization/workspace
over MasterKey auth, streams agent runs in real time, and runs the *sandbox*
(shell) and *browser* on the user's local machine via the Strobes bridge
protocols.
"""

__version__ = "0.1.0"
