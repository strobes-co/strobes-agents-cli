# Rust
/target/

# Local config / secrets (the MasterKey is stored under the user's config dir,
# never in the repo — ignore stray copies just in case)
config.json
*.env
.env

# OS / editor
.DS_Store
*.swp
*~
.idea/
.vscode/
